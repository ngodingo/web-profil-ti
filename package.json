{
  "name": "web-profil-ti",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"tailwindcss -i ./src/input.css -o ./dist/output.css --watch\" \"live-server -q --port=3000\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ngodingo",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.3.3"
  }
}
