{
  "name": "web-profil-ti",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "live-server -q --port=3000 & open-cli http://localhost:3000 & tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ngodingo",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "open-cli": "^7.2.0",
    "tailwindcss": "^3.3.3"
  }
}
